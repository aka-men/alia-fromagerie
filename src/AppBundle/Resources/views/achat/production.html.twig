<div class="row">
    <div class="col-lg-12">
    <p style="font-weight: bold;">Année : {% if params.annee is defined %}{{ params.annee }}{% else %}Tous{% endif %} | Mois : {% if params.mois is defined %}{{ params.mois }}{% else %}Tous{% endif %}</p>
    <br/>
    </div>
</div>
<div class="row">
    <div class="col-lg-12 table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
            <tr>
                {% for p in production %}
                    <th class="text-center" title="{{ p.titre}}">{{ p.titre }}</th>
                {% endfor %}
            </tr>
            </thead>
            <tbody>
            <tr>
                {% for p in production %}
                    <td class="text-center" title="{{ p.production}}">{{ p.production }}</td>
                {% endfor %}
            </tr>
            </tbody>
        </table>
    </div>
</div>

